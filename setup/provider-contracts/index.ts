// Setup provider-contract barrel. Optional provider skills append imports here;
// installed setup implementations remain in setup/providers/index.ts.
import './claude.js';

export * from './registry.js';
